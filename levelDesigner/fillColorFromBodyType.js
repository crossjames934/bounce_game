function fillColorFromBodyType(chosenBodyType) {
  if (chosenBodyType === 'block') fill(180, 180, 255);
  if (chosenBodyType === 'goal') fill(80, 100, 100);
  if (chosenBodyType === 'danger') fill(0, 100, 100);
}